{
   "name": "desafio",
   "version": "1.0.0",
   "license": "MIT",
   "devDependencies": {
      "@types/node": "^18.7.6",
      "gh-pages": "^4.0.0",
      "parcel": "^2.7.0"
   },
   "scripts": {
      "dev": "parcel serve ./src/index.html",
      "build": "parcel build ./src/index.html --dist-dir build --public-url /dwf-desafio-m5",
      "deploy": "gh-pages -d ./build",
      "live": "yarn build && yarn deploy"
   }
}
